# c

This directory is reserved for the future ANSI C shim layer over common RocksDB calls.

Nothing here is usable yet. Do not assume RocksDB headers, libraries, or generated C build inputs already exist.
